# Currency Converter Microservice (gRPC)

A high-performance microservice using **gRPC** and **Protobuf** instead of REST/JSON.

## 🚀 Key Features
- **Protocol Buffers:** Defining strict API contracts (`.proto`).
- **gRPC:** High-speed binary communication between services.
- **Code Generation:** Using `protoc` to generate Go server/client code.
- **RPC Pattern:** Calling remote functions as if they were local.

## 🛠️ Tech Stack
- **Go**, **gRPC**, **Protobuf**