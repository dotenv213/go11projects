# JWT Authentication System

A secure authentication API implementing **JWT (JSON Web Tokens)** and secure cookie handling.

## 🚀 Key Features
- **Password Hashing:** Using `bcrypt` for secure password storage.
- **JWT Implementation:** Token generation and validation (Standard `v5`).
- **Secure Cookies:** HTTPOnly cookies to prevent XSS attacks.
- **Middleware Pattern:** Protecting routes against unauthorized access.

## 🛠️ Tech Stack
- **Go**, **Fiber**, **Golang-JWT**, **Bcrypt**, **SQLite**